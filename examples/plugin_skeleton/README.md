# Plugin Skeleton

This is a minimal, no-build plugin example.

Run it from the repo root:

```bash
export AFS_PLUGIN_DIRS="$PWD/examples/plugin_skeleton"
./scripts/afs plugins --details
./scripts/afs hello
```
