name = "example_work"
description = "Example extension manifest"
api_version = 1

knowledge_mounts = ["knowledge"]
skill_roots = ["skills"]
model_registries = ["registry"]

cli_modules = []
policies = []

[hooks]
before_context_read = []
after_context_write = []
before_agent_dispatch = []

# To contribute MCP tools, declare the section with a real module:
# [mcp_tools]
# module = "example_work.mcp_tools"
# factory = "register_mcp_tools"
# catalog = "slim"
